<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <declare-styleable name="Switch_Style">
        <!-- Default style for the Switch widget. -->
        <attr name="switchStyle1" format="reference" />
    </declare-styleable>
    <declare-styleable name="SwitchButton">

        <!-- Drawable to use as the "thumb" that switches back and forth. -->
        <attr name="thumb1" format="reference" />
        <!-- Drawable to use as the "track" that the switch thumb slides within. -->
        <attr name="track1" format="reference" />
        <!-- Text to use when the switch is in the checked/"on" state. -->
        <attr name="textOn1" format="string" />
        <!-- Text to use when the switch is in the unchecked/"off" state. -->
        <attr name="textOff1" format="string" />
        <!-- Amount of padding on either side of text within the switch thumb. -->
        <attr name="thumbTextPadding1" format="dimension" />
        <!-- TextAppearance style for text displayed on the switch thumb. -->
        <attr name="switchTextAppearance1" format="reference" />
        <!-- Minimum width for the switch component -->
        <attr name="switchMinWidth1" format="dimension" />
        <!-- Minimum space between the switch and caption text -->
        <attr name="switchPadding1" format="dimension" />
    </declare-styleable>
    
    <declare-styleable name="TextAppearance">
        <!-- Text color. -->
        <attr name="textColor1" format="reference|color" />
        <!-- Size of the text. Recommended dimension type for text is "sp" for scaled-pixels (example: 15sp). -->
        <attr name="textSize1" format="dimension" />
        <!-- Style (bold, italic, bolditalic) for the text. -->
        <attr name="textStyle1">
            <flag name="norma1" value="0" />
            <flag name="bold" value="1" />
            <flag name="italic" value="2" />
        </attr>
        <!-- Typeface (normal, sans, serif, monospace) for the text. -->
        <attr name="typeface">
            <enum name="normal" value="0" />
            <enum name="sans" value="1" />
            <enum name="serif" value="2" />
            <enum name="monospace" value="3" />
        </attr>
        <!-- Color of the text selection highlight. -->
        <attr name="textColorHighlight" format="reference|color" />
        <!-- Color of the hint text. -->
        <attr name="textColorHint" format="reference|color" />
        <!-- Color of the links. -->
        <attr name="textColorLink" format="reference|color" />
        <!-- Present the text in ALL CAPS. This may use a small-caps form when available. -->
        <attr name="textAllCaps" format="boolean" />
    </declare-styleable>

    <style name="Switch">
        <item name="track1">@drawable/switch_btn</item>
        <item name="thumb1">@drawable/kuang7_23</item>
        <item name="switchTextAppearance1">@android:style/TextAppearance.Small</item>
        <item name="textOn1">@string/switch_on</item>
        <item name="textOff1">@string/switch_off</item>
        <item name="thumbTextPadding1">16dip</item>
        <item name="switchMinWidth1">80dip</item>
        <item name="switchPadding1">10dip</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">#00FFFFFF</item>
    </style>

    <string name="switch_on">On</string>
    <string name="switch_off">Off</string>
    <color name="switchtextcolor">#FFFFFF</color>
</resources>